services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio_back
    restart: always
    env_file:
      - .env
    ports:
      - "127.0.0.1:1047:1047"
